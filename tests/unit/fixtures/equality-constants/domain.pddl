(define (domain equality)
  (:requirements :typing :equality)
  (:types thing)
  (:constants c - thing)
  (:predicates (p))
  (:action same
    :parameters (?a ?b - thing)
    :precondition (= ?a ?b)
    :effect (p))
)
